# SQL - ORDER BY & GROUP BY 정리

---

## 1. ORDER BY

- ORDER BY는 **조회 결과를 정렬하는 기능**이다.

- [확실] ORDER BY는 SELECT 결과 정렬을 위한 표준 SQL 문법
- [추정] 내부 정렬 방식은 DBMS마다 다름

---

## 2. GROUP BY

- GROUP BY는 **특정 컬럼 기준으로 데이터를 그룹화**한다.
- 주로 **집계 함수(SUM, COUNT 등)**와 함께 사용된다.

- [확실] GROUP BY는 데이터를 그룹 단위로 묶는 역할
- [확실] 집계 함수와 함께 사용하는 것이 일반적

---

## 3. 전체 문법 구조

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼명
HAVING 조건
ORDER BY 컬럼명
LIMIT 숫자;
```

- [확실] SQL 기본 구조 및 작성 순서
- [추정] LIMIT은 MySQL/PostgreSQL 문법 (Oracle은 다름)

---

## 4. ORDER BY 사용법

### 4.1 오름차순 (ASC)

```sql
SELECT * 
FROM 테이블명 
ORDER BY 컬럼명 ASC;
```

- ASC는 생략 가능

---

### 4.2 내림차순 (DESC)

```sql
SELECT * 
FROM 테이블명 
ORDER BY 컬럼명 DESC;
```

---

### 4.3 다중 컬럼 정렬

```sql
SELECT * 
FROM 테이블명 
ORDER BY 컬럼1 ASC, 컬럼2 DESC;
```

- 앞 컬럼 기준 정렬 후, 동일 값이면 다음 컬럼 기준 정렬

- [확실] ORDER BY는 다중 컬럼 정렬 지원

---

### 4.4 조건 + 정렬

```sql
SELECT * 
FROM 테이블명 
WHERE 조건
ORDER BY 컬럼명;
```

---

### 4.5 조회 제한 (LIMIT)

```sql
SELECT * 
FROM 테이블명 
LIMIT 숫자;
```

- ❗ 기존 오류 수정
    - `lIMIT` → `LIMIT`
    - 백틱 종료 오류 수정

- [확실] LIMIT은 결과 행 개수 제한
- [추정] 일부 DBMS에서는 OFFSET과 함께 사용

---

## 5. 집계 함수 (GROUP BY와 함께 사용)

| 함수 | 설명 |
|------|------|
| SUM() | 합계 |
| AVG() | 평균 |
| MIN() | 최소값 |
| MAX() | 최대값 |
| COUNT() | 행 개수 |
| COUNT(DISTINCT 컬럼) | 중복 제거 후 개수 |

- [확실] 집계 함수는 그룹 단위 계산 수행

---

## 6. GROUP BY 사용 예시

### 6.1 기본 그룹화

```sql
SELECT 컬럼명
FROM 테이블명
GROUP BY 컬럼명;
```

---

### 6.2 조건 → 그룹화

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼명;
```

---

### 6.3 그룹화 → 조건 (HAVING)

```sql
SELECT 컬럼명
FROM 테이블명
GROUP BY 컬럼명
HAVING 조건;
```

- [확실] HAVING은 그룹 결과에 대한 조건

---

### 6.4 WHERE + GROUP BY + HAVING

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼명
HAVING 조건;
```

---

### 6.5 그룹화 + 정렬

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼명
HAVING 조건
ORDER BY 컬럼명;
```

---

## 7. WHERE vs HAVING 차이

| 구분 | WHERE | HAVING |
|------|-------|--------|
| 실행 시점 | GROUP BY 이전 | GROUP BY 이후 |
| 대상 | 개별 행(Row) | 그룹 |
| 집계 함수 사용 | 불가능 | 가능 |

- [확실] WHERE와 HAVING의 차이는 SQL 핵심 개념
- [추정] 일부 DBMS는 내부 최적화로 다르게 처리 가능

---

## 8. SQL 실행 순서

```text
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT
```

- [확실] SQL의 논리적 실행 순서
- [추정] 실제 실행 계획은 옵티마이저에 의해 변경될 수 있음

---

## 9. 핵심 정리

- ORDER BY → 정렬
- GROUP BY → 그룹화
- HAVING → 그룹 조건
- LIMIT → 결과 제한

- [확실] 이 4개는 SELECT 심화에서 가장 중요한 개념
- [추정] 성능은 인덱스, 데이터 양에 크게 영향 받음

---

## 참고

- [확실] GROUP BY + HAVING 조합은 집계 쿼리의 핵심 패턴
- [불확실] DBMS별 문법 차이 존재 (특히 LIMIT, OFFSET)
