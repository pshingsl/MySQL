# MySQL 데이터 형식 정리

---

## 1. 데이터 형식 개요

- 데이터 형식(Data Type)은 **테이블 생성 시 컬럼에 저장될 데이터의 종류와 범위를 정의하는 것**이다.

- [확실] 데이터 타입은 저장 가능한 값과 메모리 크기를 결정
- [추정] DBMS마다 데이터 타입 및 범위는 일부 차이 존재

---

## 2. 정수형 (Integer)

- 소수점이 없는 숫자를 저장할 때 사용
- 예: 인원 수, 가격, 수량

| 데이터형식 | 바이트 수 | 범위 (Signed 기준) |
|------------|----------|--------------------|
| TINYINT    | 1        | -128 ~ 127 |
| SMALLINT   | 2        | -32,768 ~ 32,767 |
| INT        | 4        | 약 -21억 ~ 21억 |
| BIGINT     | 8        | 매우 큰 정수 (약 ±9경) |

- ❗ 기존 오류 수정
    - BIGINT 뒤의 `Q` 제거
    - 범위 누락 보완

- [확실] 정수형은 저장 크기에 따라 표현 범위 결정
- [추정] UNSIGNED 사용 시 양수 범위 2배 확장

---

## 3. 문자형 (String)

- 문자열 데이터를 저장할 때 사용

| 데이터형식 | 설명 |
|------------|------|
| CHAR(n)    | 고정 길이 문자열 |
| VARCHAR(n) | 가변 길이 문자열 |

---

### CHAR vs VARCHAR

- CHAR
    - 고정 길이
    - 지정 길이보다 짧으면 **공간 낭비 발생**

- VARCHAR
    - 가변 길이
    - 필요한 만큼만 저장 → **공간 효율적**

- ❗ 기존 설명 보완  
  → "VARCHAR보다 CHAR가 무조건 성능 좋다" ❌  
  → "짧고 고정된 데이터는 CHAR가 유리할 수 있음" ✅

- [확실] VARCHAR는 가변 길이 저장 방식
- [추정] 성능 차이는 데이터 특성 및 인덱스 상황에 따라 다름

---

## 4. 실수형 (Floating Point)

- 소수점이 있는 숫자를 저장

| 데이터형식 | 바이트 수 | 설명 |
|------------|----------|------|
| FLOAT      | 4        | 단정밀도 |
| DOUBLE     | 8        | 배정밀도 |

- [확실] FLOAT/DOUBLE은 근사값 저장 (정확도 손실 가능)
- [추정] 정확한 계산은 DECIMAL 사용 권장

---

## 5. 날짜형 (Date/Time)

- 날짜 및 시간 데이터를 저장

| 데이터형식 | 바이트 수 | 설명 |
|------------|----------|------|
| DATE       | 3        | 날짜 (YYYY-MM-DD) |
| TIME       | 3        | 시간 (HH:MM:SS) |
| DATETIME   | 8        | 날짜 + 시간 |

---

### 특징

- DATE → 날짜만 저장
- TIME → 시간만 저장
- DATETIME → 날짜 + 시간 함께 저장

- ❗ 오타 수정
    - "떄" → "때"

- [확실] 날짜형 타입은 시간 데이터 관리에 필수
- [추정] TIMESTAMP는 시간대(Timezone) 영향을 받음

---

## 6. 추가로 알아두면 좋은 타입

### DECIMAL (중요)

```sql
DECIMAL(10,2)
```

- 정확한 숫자 계산 (금액 등)에 사용

- [확실] 금융 데이터는 FLOAT 대신 DECIMAL 사용 권장

---

## 7. 핵심 정리

- 정수형 → INT, BIGINT
- 문자형 → CHAR, VARCHAR
- 실수형 → FLOAT, DOUBLE
- 날짜형 → DATE, DATETIME

- [확실] 데이터 타입 선택은 성능과 저장 효율에 큰 영향
- [추정] 실무에서는 VARCHAR + INT + DATETIME 조합이 가장 많이 사용

---

## 참고

- [확실] MySQL 공식 문서 기준 데이터 타입 정의 존재
- [불확실] DBMS 버전에 따라 내부 구현 차이 가능  
