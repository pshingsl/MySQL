# SQL 기본 문법 - 조회 (SELECT)

---

## 1. SELECT 문 개요

- SELECT 문은 **테이블에서 데이터를 조회(추출)** 하는 SQL 명령어이다.

- [확실] SELECT는 데이터 조회를 위한 표준 SQL 명령어
- [추정] "추출"이라는 표현은 조회 의미를 쉽게 설명한 것

---

## 2. 기본 구조

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 조건;
```

- SELECT : 조회할 컬럼 지정
- FROM : 데이터를 가져올 테이블 지정
- WHERE : 조건 필터링

- [확실] SELECT-FROM-WHERE 구조는 SQL의 기본 문법

---

## 3. 동작 원리 (논리적 실행 순서)

```text
FROM → WHERE → SELECT
```

1. FROM : 테이블 확인
2. WHERE : 조건에 맞는 데이터 필터링
3. SELECT : 최종 컬럼 선택

- [확실] SQL은 작성 순서와 실행 순서가 다름
- [추정] 실제 DB 내부 실행은 최적화 과정에서 달라질 수 있음

---

## 4. 다양한 조회 예시

### 4.1 전체 조회

```sql
SELECT * FROM 테이블명;
```

- 모든 컬럼 조회

- [확실] * 는 전체 컬럼 의미

---

### 4.2 특정 컬럼 조회

```sql
SELECT 이름, 나이 FROM 회원;
```

- 필요한 컬럼만 선택

- [확실] 컬럼 선택 조회는 성능상 유리

---

### 4.3 조건 조회

```sql
SELECT * 
FROM 회원
WHERE 나이 >= 20;
```

- 조건에 맞는 데이터만 조회

- [확실] WHERE는 행(Row) 필터링 역할

---

### 4.4 별칭 사용 (AS)

```sql
SELECT 이름 AS user_name
FROM 회원;
```

- 컬럼 이름을 다른 이름으로 표시

- [확실] AS는 별칭(alias) 지정

---

### 4.5 중복 제거

```sql
SELECT DISTINCT 지역
FROM 회원;
```

- 중복 데이터 제거

- [확실] DISTINCT는 중복 제거 키워드

---

## 5. 조건식 (WHERE)

### 비교 연산자

- = (같다)
- != 또는 <> (다르다)
- >, <, >=, <=

---

### 논리 연산자

```sql
WHERE 나이 >= 20 AND 성별 = '남자';
```

- AND : 모두 만족
- OR : 하나 이상 만족
- NOT : 조건 부정

- [확실] SQL 표준 연산자

---

### 범위 조건

```sql
WHERE 나이 BETWEEN 20 AND 30;
```

---

### 목록 조건

```sql
WHERE 지역 IN ('서울', '부산');
```

---

### 패턴 검색

```sql
WHERE 이름 LIKE '김%';
```

- % : 모든 문자

- [확실] LIKE는 문자열 패턴 검색

---

## 6. 핵심 정리

- SELECT : 데이터 조회
- FROM : 대상 테이블
- WHERE : 조건 필터링
- 실행 순서 : FROM → WHERE → SELECT

- [확실] SELECT 문은 SQL에서 가장 기본이 되는 핵심 문법
- [추정] 실제 성능은 인덱스, 실행계획 등에 따라 달라짐

---

## 참고

- [확실] SQL은 ANSI/ISO 표준 기반 언어
- [불확실] DBMS별 문법 차이 존재 (MySQL, Oracle 등)
