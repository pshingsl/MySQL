# 데이터베이스 생성 및 SQL 기초 정리



---

## 1. 데이터베이스 만들기

- 데이터베이스는 데이터를 저장하는 공간이다.

- [확실] 데이터베이스는 구조화된 데이터 저장소라는 정의는 표준적
- [추정] “공간”이라는 표현은 개념 설명을 단순화한 것

---

## 2. 데이터베이스 구축 절차

1. 데이터베이스(스키마) 생성
2. 테이블 생성
3. 데이터 입력 / 수정 / 삭제
4. 데이터 조회 및 활용

- [확실] DB 구축 흐름은 일반적인 개발 프로세스와 일치
- [추정] 실제 서비스에서는 설계(모델링)가 선행됨

---

## 3. 데이터베이스 다루기 (DDL)

### 3.1 데이터베이스 조회

```sql
SHOW DATABASES;
```

---

### 3.2 데이터베이스 삭제

```sql
DROP DATABASE IF EXISTS 데이터베이스명;
```

- ❗ 기존 작성 오류: `exist → exists`

- [확실] MySQL 문법 기준으로 IF EXISTS 사용

---

### 3.3 데이터베이스 생성

```sql
CREATE DATABASE 데이터베이스명;
```

---

### 3.4 데이터베이스 선택

```sql
USE 데이터베이스명;
```

---

### 3.5 스키마 생성

```sql
CREATE SCHEMA 데이터베이스명;
```

- [확실] MySQL에서는 DATABASE와 SCHEMA는 거의 동일 의미

---

## 4. 테이블 생성

```sql
CREATE TABLE 테이블명 (
    컬럼이름1 데이터타입 NOT NULL,
    컬럼이름2 데이터타입,
    컬럼이름3 데이터타입
);
```

- [확실] 테이블 생성 시 컬럼명 + 데이터타입 필수

---

## 5. 테이블 제약 조건

- NOT NULL : NULL 값 허용 안함
- UNIQUE : 중복 값 허용 안함
- PRIMARY KEY : NOT NULL + UNIQUE
- FOREIGN KEY : 다른 테이블을 참조
- DEFAULT : 기본값 설정
- AUTO_INCREMENT : 자동 증가

- [확실] 제약 조건은 데이터 무결성 보장
- ❗ 기존 오류 수정: FOREIGN KEY 설명  
  → “의존하면 안 된다” ❌  
  → “다른 테이블을 참조한다” ✅

---

## 6. 테이블 구조 수정 (ALTER)

### 6.1 컬럼 추가

```sql
ALTER TABLE 테이블명 ADD 컬럼명 데이터타입;
```

---

### 6.2 컬럼 삭제

```sql
ALTER TABLE 테이블명 DROP COLUMN 컬럼명;
```

---

### 6.3 컬럼 이름 + 타입 변경

```sql
ALTER TABLE 테이블명 CHANGE 기존컬럼명 새컬럼명 데이터타입;
```

---

### 6.4 컬럼 타입 변경

```sql
ALTER TABLE 테이블명 MODIFY 컬럼명 데이터타입;
```

---

### 6.5 기본키 추가

```sql
ALTER TABLE 테이블명 ADD CONSTRAINT pk_name PRIMARY KEY (컬럼명);
```

---

### 6.6 외래키 추가

```sql
ALTER TABLE 테이블명 
ADD FOREIGN KEY (컬럼명) 
REFERENCES 참조테이블(참조컬럼);
```

---

### 6.7 외래키 삭제

```sql
ALTER TABLE 테이블명 DROP FOREIGN KEY 외래키이름;
```

---

### 6.8 테이블 이름 변경

```sql
ALTER TABLE 기존테이블명 RENAME TO 새테이블명;
```

---

- ❗ 기존 오류 전부 수정:  
  `AFTER TABLE → ALTER TABLE`

- [확실] ALTER TABLE은 테이블 구조 변경 표준 명령어

---

## 7. 데이터 조작 (DML)

### 7.1 데이터 입력

```sql
INSERT INTO 테이블명 (컬럼1, 컬럼2)
VALUES (값1, 값2);
```

또는

```sql
INSERT INTO 테이블명
VALUES (값1, 값2);
```

---

### 7.2 데이터 수정

```sql
UPDATE 테이블명
SET 컬럼1 = 값1
WHERE 조건;
```

- ❗ WHERE 없으면 전체 데이터 변경됨

- [확실] UPDATE는 조건 없이 실행 시 전체 row 변경

---

### 7.3 데이터 삭제

#### 테이블 삭제

```sql
DROP TABLE 테이블명;
```

#### 데이터베이스 삭제

```sql
DROP DATABASE 데이터베이스명;
```

---

### 7.4 데이터 삭제 (복구 가능)

```sql
DELETE FROM 테이블명 WHERE 조건;
```

- [추정] 일부 DBMS에서 트랜잭션 사용 시 롤백 가능

---

### 7.5 데이터 전체 삭제 (복구 불가)

```sql
TRUNCATE TABLE 테이블명;
```

- [확실] TRUNCATE는 빠르고 롤백 불가 (대부분 DBMS 기준)

---

## 8. 데이터 조회 (SELECT)

```sql
SELECT 컬럼명, 집계함수 AS 별명
FROM 테이블명
WHERE 조건
GROUP BY 컬럼명
HAVING 그룹조건
ORDER BY 컬럼명;
```

- ❗ 기존 오타 수정
    - 컬러명 → 컬럼명
    - ORDER BY 컬러면 → 컬럼명

---

## 9. SQL 실행 순서

실제 실행 순서:

1. FROM
2. WHERE
3. GROUP BY
4. HAVING
5. SELECT
6. ORDER BY

- [확실] SQL의 논리적 실행 순서는 위와 같음
- [추정] 내부 최적화에 따라 물리적 실행 순서는 달라질 수 있음

---

## 10. 핵심 정리

- DDL: CREATE, ALTER, DROP
- DML: INSERT, UPDATE, DELETE
- 조회: SELECT
- 실행 순서: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY

- [확실] SQL 기본 구조 및 분류는 표준 정의
- [추정] DBMS마다 문법 일부 차이 존재

---

## 참고

- [확실] SQL은 ANSI/ISO 표준 기반 언어
- [불확실] DBMS별 확장 문법(MySQL, Oracle 등)은 상이  
