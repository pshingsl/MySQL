# 테이블 제약조건 (Constraint)

---

# 1. 테이블 제약조건

- 테이블 생성 시 데이터의 정확성과 일관성을 유지하기 위해 제약조건(Constraint)을 설정한다.

### 대표적인 제약조건

- PRIMARY KEY (기본 키)
- FOREIGN KEY (외래 키)
- UNIQUE (고유 키)
- CHECK (체크 제약조건)
- DEFAULT (기본값)
- NOT NULL (NULL 허용 금지)

- 제약조건은 데이터 무결성을 보장하기 위한 핵심 기능


---

# 2. 데이터 무결성

- 데이터 무결성(Data Integrity)은 데이터가 **정확하고 일관성 있게 유지되는 상태**를 의미한다.

- ❗ 기존 오류 수정  
  → "데이터 결합이 없음" ❌  
  → "정확성과 일관성 유지" ✅

### 예시

- 동일한 회원 아이디가 중복 생성되면 데이터 무결성이 깨진다.

-  PRIMARY KEY, UNIQUE 등은 무결성 보장을 위해 사용됨

---

# 3. 기본 키 제약조건 (PRIMARY KEY)

- 기본 키는 테이블에서 각 데이터를 구분하는 고유 식별자이다.

### 예시

- 회원 테이블 → 회원 ID
- 학생 테이블 → 학번
- 직원 테이블 → 사번

---

## 특징

- 중복 불가능
- NULL 허용 불가
- 테이블당 1개만 설정 가능

-  PRIMARY KEY = UNIQUE + NOT NULL 특징 포함

---

## 문법

```sql
CREATE TABLE 테이블명 (
    id INT PRIMARY KEY,
    이름 VARCHAR(30)
);
```

또는

```sql
CREATE TABLE 테이블명 (
    id INT NOT NULL,
    이름 VARCHAR(30),
    PRIMARY KEY(id)
);
```

---

## 주의점

- 실무에서는 대부분의 테이블에 기본 키를 설정한다.

-  PK 없는 테이블도 생성 가능


---

# 4. 외래 키 제약조건 (FOREIGN KEY)

- 외래 키는 두 테이블 간 관계를 연결하는 제약조건이다.

- 다른 테이블의 PRIMARY KEY를 참조한다.

---

## 관계 예시

- 회원 테이블 ← 주문 테이블

```text
회원(id)
주문(member_id)
```

- 주문.member_id → 회원.id 참조

---

## 문법

```sql
FOREIGN KEY (컬럼명)
REFERENCES 참조테이블(참조컬럼);
```

---

## 특징

- 존재하지 않는 참조값 입력 방지
- 데이터 관계 유지

- ❗ 기존 오류 수정  
  → 외래 키를 가진 테이블은 "참조 테이블" ❌  
  → "자식(Child) 테이블" 또는 "외래 키 테이블" ✅

-  FOREIGN KEY는 참조 무결성 보장

---

# 5. CASCADE 옵션

- 참조 중인 데이터 변경 시 자동 반영 기능

---

## ON UPDATE CASCADE

- 부모 테이블의 PK 변경 시 자식 테이블도 자동 변경

```sql
ON UPDATE CASCADE
```

---

## ON DELETE CASCADE

- 부모 데이터 삭제 시 자식 데이터도 함께 삭제

```sql
ON DELETE CASCADE
```

-  CASCADE는 참조 무결성 유지 기능


---

# 6. 고유 키 제약조건 (UNIQUE)

- 중복되지 않는 유일한 값을 저장하는 제약조건

### 예시

- 이메일
- 휴대폰 번호

---

## 특징

| 항목 | PRIMARY KEY | UNIQUE |
|------|-------------|--------|
| 중복 허용 | 불가능 | 불가능 |
| NULL 허용 | 불가능 | 가능 |
| 개수 제한 | 테이블당 1개 | 여러 개 가능 |

-  UNIQUE는 NULL 허용 가능 (DBMS별 차이 일부 존재)

---

## 문법

```sql
CREATE TABLE 테이블명 (
    email VARCHAR(100) UNIQUE
);
```

---

# 7. 체크 제약조건 (CHECK)

- 입력되는 데이터가 특정 조건을 만족하는지 검사

### 예시

- 나이 >= 0
- 비밀번호 길이 제한

---

## 문법

```sql
CREATE TABLE 테이블명 (
    나이 INT CHECK (나이 >= 0)
);
```

-  CHECK는 조건 기반 검증 기능

---

# 8. 기본값 정의 (DEFAULT)

- 값을 입력하지 않았을 때 자동으로 저장될 기본값 설정

---

## 문법

```sql
CREATE TABLE 테이블명 (
    상태 VARCHAR(20) DEFAULT 'ACTIVE'
);
```

- ❗ 기존 오류 수정  
  → DEFAULT 뒤에 기본값 필요

-  DEFAULT는 값 생략 시 자동 적용

---

# 9. NOT NULL

- NULL 값 입력을 허용하지 않는 제약조건

```sql
이름 VARCHAR(30) NOT NULL
```

-  필수 입력 컬럼에 사용

---

# 10. 핵심 정리

| 제약조건 | 역할 |
|----------|------|
| PRIMARY KEY | 고유 식별자 |
| FOREIGN KEY | 테이블 관계 연결 |
| UNIQUE | 중복 방지 |
| CHECK | 조건 검증 |
| DEFAULT | 기본값 지정 |
| NOT NULL | NULL 금지 |

-  제약조건은 데이터 무결성 유지 핵심 요소
---

# 참고

-  제약조건은 관계형 데이터베이스 핵심 개념
 
