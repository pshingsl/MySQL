# 데이터 변경을 위한 SQL (INSERT, UPDATE, DELETE)

> **작성일시:** 2026-04-02 오후 2:31

---

## 1. 개요

- 데이터 변경을 위한 SQL은 **DML(Data Manipulation Language)**에 해당한다.

### 주요 명령어
- INSERT : 데이터 입력
- UPDATE : 데이터 수정
- DELETE : 데이터 삭제

- [확실] DML은 데이터 조작을 위한 SQL 분류
- [확실] INSERT, UPDATE, DELETE는 대표적인 DML

---

## 2. 데이터 입력 (INSERT)

- 테이블에 새로운 행(Row)을 추가하는 명령어이다.

### 기본 문법

```sql
INSERT INTO 테이블명 (열1, 열2, ...)
VALUES (값1, 값2, ...);
```

---

### 전체 컬럼 입력

```sql
INSERT INTO 테이블명
VALUES (값1, 값2, 값3, ...);
```

- ❗ 컬럼 순서와 값 순서가 반드시 일치해야 함

- [확실] INSERT는 새로운 데이터 추가
- [추정] 일부 DBMS는 DEFAULT 값 자동 처리

---

## 3. 데이터 수정 (UPDATE)

- 기존 데이터를 변경하는 명령어이다.

### 기본 문법

```sql
UPDATE 테이블명
SET 열1 = 값1, 열2 = 값2
WHERE 조건;
```

---

### WHERE 절의 중요성

- WHERE 조건이 없으면 **모든 행이 변경됨**

```sql
UPDATE 테이블명
SET 열1 = 값1;
```

- ⚠️ 전체 데이터 변경 (주의)

---

### 사용 시 주의

- 테스트 환경 또는 초기 데이터일 경우에만 전체 UPDATE 사용 권장

- [확실] WHERE 없이 UPDATE 실행 시 전체 데이터 변경
- [확실] 실무에서는 매우 위험한 작업으로 간주

---

## 4. 데이터 삭제 (DELETE)

- 테이블의 특정 행(Row)을 삭제하는 명령어이다.

### 기본 문법

```sql
DELETE FROM 테이블명
WHERE 조건;
```

---

### 전체 데이터 삭제

```sql
DELETE FROM 테이블명;
```

- ⚠️ 테이블 구조는 남고 데이터만 삭제됨

---

### DELETE vs TRUNCATE

| 구분 | DELETE | TRUNCATE |
|------|--------|----------|
| 조건 사용 | 가능 | 불가능 |
| 속도 | 느림 | 빠름 |
| 롤백 | 가능 (일부 DBMS) | 대부분 불가능 |

- [확실] DELETE는 행 단위 삭제
- [확실] TRUNCATE는 전체 데이터 빠르게 삭제
- [추정] 롤백 가능 여부는 DBMS 및 설정에 따라 다름

---

## 5. 핵심 정리

- INSERT → 데이터 추가
- UPDATE → 데이터 수정 (WHERE 필수)
- DELETE → 데이터 삭제 (조건 중요)

- [확실] DML은 CRUD 중 Create, Update, Delete 담당
- [추정] 성능 및 안전성은 트랜잭션 관리에 영향 받음

---

## 참고

- [확실] SQL은 ANSI/ISO 표준 기반
- [불확실] DBMS별 동작 차이 존재 (특히 트랜잭션 처리)
