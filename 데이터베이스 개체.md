# 데이터베이스 개체 (Index, View, Stored Procedure)

> **작성 목적:** 맛보기 개념 정리 (추후 상세 학습 예정)

---

## 1. 데이터베이스 개체 개요

- 테이블은 데이터베이스의 핵심 개체이다.
- 하지만 실제 데이터베이스에서는 다음과 같은 다양한 객체가 함께 사용된다.

### 주요 개체

- 인덱스 (Index)
- 뷰 (View)
- 스토어드 프로시저 (Stored Procedure)
- 트리거 (Trigger)
- 함수 (Function)
- 커서 (Cursor)

- [확실] 위 객체들은 대부분의 RDBMS(MySQL, Oracle 등)에서 제공
- [추정] 모든 DBMS가 동일한 기능/문법을 지원하지는 않음

---

## 2. 인덱스 (Index)

### 개념

- 인덱스는 **데이터 조회 속도를 향상시키기 위한 자료구조**이다.
- 데이터가 많아질수록 검색 속도가 느려지는데, 이를 개선하기 위해 사용된다.
- 특정 컬럼을 기준으로 **빠르게 데이터를 찾을 수 있도록 도와준다.**

- [확실] 인덱스는 검색 성능 향상을 위해 사용됨
- [확실] 일반적으로 B-Tree 기반 구조 사용
- [추정] 내부 구현은 DBMS마다 다름 (Hash Index 등 존재)

---

### 인덱스 생성

```sql
CREATE INDEX 인덱스이름
ON 테이블명(컬럼명);
```

- [확실] 기본 인덱스 생성 문법

---

### 주의점

- 조회 성능 ↑
- 삽입/수정/삭제 성능 ↓

- [확실] 인덱스는 쓰기 성능에 오버헤드 발생

---

## 3. 뷰 (View)

### 개념

- 뷰는 **가상의 테이블**이다.
- 실제 데이터를 저장하지 않고, **SELECT 쿼리 결과를 보여주는 객체**이다.

- [확실] 뷰는 물리적 데이터를 저장하지 않음 (일반 View 기준)
- [추정] 일부 DBMS는 Materialized View 제공 (데이터 저장됨)

---

### 특징

- 사용자 입장에서는 테이블처럼 사용 가능
- 실제 데이터는 원본 테이블에 존재
- 보안 및 복잡한 쿼리 단순화에 사용

- ❗ 기존 오류 보완  
  → "테이블과 완전히 동일" ❌  
  → "사용 방식은 유사하지만 내부 구조는 다름" ✅

---

### 뷰 생성

```sql
CREATE VIEW 뷰이름 AS
SELECT 컬럼명
FROM 테이블명
WHERE 조건;
```

- ❗ 기존 오류 수정:  
  `CREATE VIEW 뷰이름;` ❌ → SELECT 필요

---

## 4. 스토어드 프로시저 (Stored Procedure)

### 개념

- 스토어드 프로시저는 **여러 SQL문을 하나로 묶어 실행하는 프로그램 단위**이다.
- DB 내부에서 실행되는 일종의 함수/프로그램이다.

- [확실] MySQL 등에서 제공하는 서버 사이드 로직 기능
- [확실] 반복문, 조건문 사용 가능

---

### 특징

- SQL을 재사용 가능
- 복잡한 로직 처리 가능
- 네트워크 트래픽 감소

- [확실] 서버에서 실행되므로 성능 이점 존재

---

### 간단 예시

```sql
DELIMITER $$

CREATE PROCEDURE 프로시저명()
BEGIN
    SELECT * FROM 테이블명;
END $$

DELIMITER ;
```

- [추정] 문법은 DBMS(MySQL, Oracle 등)에 따라 차이 있음

---

## 5. 핵심 정리

- 인덱스 → 조회 속도 향상
- 뷰 → 가상 테이블 (SELECT 결과)
- 프로시저 → SQL 묶음 (로직 처리)

- [확실] 위 3개는 실무에서 매우 자주 사용되는 DB 객체
- [추정] 성능 최적화 시 인덱스 활용이 핵심

---

## 참고

- [확실] 인덱스는 B-Tree 구조 기반이 일반적
- [불확실] 최신 DBMS에서는 다양한 인덱스 구조 지원 (Bitmap, Hash 등)  
