# 데이터베이스 기초 정리

## 1. 데이터베이스란?

- 데이터베이스(Database, DB)는 **데이터의 집합**이다.
- 단순한 파일 저장이 아니라, **체계적으로 저장되고 관리되는 데이터 모음**을 의미한다.

- [확실] 데이터베이스는 조직적으로 구조화된 데이터 집합이라는 정의는 학계 및 산업에서 널리 사용됨
- [추정] "데이터의 집합"이라는 표현은 입문 설명으로 단순화된 개념

---

## 2. DBMS(Database Management System)

- DBMS는 데이터베이스를 **관리하고 운영하는 소프트웨어**이다.
- 사용자는 DBMS를 통해 데이터를 저장, 조회, 수정, 삭제할 수 있다.

### 주요 역할
- 데이터 저장 및 관리
- 데이터 무결성 유지
- 동시성 제어
- 보안 관리

- [확실] DBMS는 데이터 관리 시스템으로 정의되며 위 기능은 표준적인 역할
- [추정] 모든 DBMS가 동일한 기능 수준을 제공하는 것은 아님

---

## 3. DBMS 종류

대표적인 DBMS:

- MariaDB
- OracleDB
- MySQL
- PostgreSQL

- [확실] 위 DBMS들은 대표적인 관계형 DBMS로 널리 사용됨
- [불확실] 시장 점유율 및 인기 순위는 시점에 따라 변동 가능

---

## 4. SQL (Structured Query Language)

- SQL은 DBMS에서 데이터를 **정의하고 조작하기 위한 표준 언어**이다.

### 주요 기능

- 데이터 정의 (DDL)
- 데이터 조작 (DML)
- 데이터 제어 (DCL)

- [확실] SQL은 ANSI/ISO 표준 언어로 정의되어 있음
- [추정] DBMS마다 SQL 문법에 일부 차이가 존재

---

## 5. 관계형 DBMS (RDBMS)

- RDBMS는 데이터를 **테이블 형태로 관리하는 DBMS**이다.

### 특징

- 테이블(Table)을 최소 단위로 사용
- 행(Row)과 열(Column) 구조
- 관계(Relation)를 기반으로 데이터 연결

### 테이블 구조

| 컬럼 (Column) | 설명 |
|--------------|------|
| 열(Column)   | 속성(Attribute) |
| 행(Row)      | 데이터 한 건 (Record) |

- [확실] RDBMS는 테이블 기반 구조를 사용하며 이는 관계형 모델의 핵심
- [확실] 행과 열 구조는 관계형 데이터 모델의 기본 구성 요소
- [추정] 모든 시스템에서 관계를 적극적으로 활용하는 것은 아님

---

## 6. 핵심 정리

- 데이터베이스 = 데이터의 집합
- DBMS = 데이터베이스를 관리하는 소프트웨어
- SQL = 데이터베이스를 다루는 언어
- RDBMS = 테이블 기반 데이터베이스 시스템

- [확실] 위 개념 구조는 데이터베이스 기초 개념으로 표준적으로 사용됨
- [추정] 실제 서비스에서는 NoSQL 등 다양한 DB 구조도 함께 사용됨

---

## 참고

- [확실] 관계형 데이터베이스 개념은 E.F. Codd의 관계형 모델 이론 기반
- [불확실] 최신 DB 트렌드는 계속 변화 중 (NoSQL, NewSQL 등)
