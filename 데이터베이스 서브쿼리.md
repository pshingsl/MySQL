# 서브쿼리 (Subquery)

---

# 1. 서브쿼리란?

- 서브쿼리(Subquery)는 **하나의 SQL문 내부에 포함된 또 다른 SELECT문**이다.

- 서브쿼리를 포함하는 쿼리 → 외부 쿼리(Outer Query)
- 내부에 포함된 쿼리 → 내부 쿼리(Inner Query)

-  서브쿼리는 SQL 내부에 중첩된 SELECT 문
-  괄호 `()` 안에서 작성

---

# 2. 실행 순서

```text
서브쿼리 실행 → 외부 쿼리 실행
```

- 내부 쿼리 결과를 먼저 구한 후,
- 외부 쿼리에서 해당 결과를 사용한다.

-  일반적인 서브쿼리는 내부 쿼리 먼저 실행


---

# 3. 기본 문법

```sql
SELECT 컬럼명
FROM 테이블명
WHERE 컬럼명 IN (
    SELECT 컬럼명
    FROM 테이블명
);
```

-  서브쿼리는 WHERE 절에서 자주 사용됨

---

# 4. 서브쿼리 특징

- 괄호 `()` 안에 작성
- SELECT 문 기반
- 하나의 SQL 내부에 포함됨

-  서브쿼리는 독립 실행 가능한 SELECT 문 형태

---

# 5. 메인쿼리와 서브쿼리 관계

## 일반 서브쿼리

- 메인쿼리 → 서브쿼리 컬럼 사용 가능
- 서브쿼리 → 메인쿼리 컬럼 사용 불가능

---

## 상관 서브쿼리 (Correlated Subquery)

```sql
SELECT 이름
FROM 회원 A
WHERE EXISTS (
    SELECT 1
    FROM 주문 B
    WHERE A.id = B.member_id
);
```

- 서브쿼리에서 메인쿼리 컬럼 사용 가능

- ❗ 기존 설명 보완  
  → 일반 서브쿼리 기준으로만 설명되어 있었음

-  상관 서브쿼리는 외부 컬럼 참조 가능

---

# 6. 서브쿼리 장점

## 6.1 가독성 향상

- 복잡한 로직을 단계별로 작성 가능

---

## 6.2 복잡한 쿼리 처리 가능

- JOIN, 집계 함수와 함께 활용 가능

---

## 6.3 유지보수 편리

- 쿼리 구조 분리가 가능


---

# 7. 서브쿼리 종류

---

## 7.1 중첩 서브쿼리 (Nested Subquery)

- WHERE 절에 사용하는 서브쿼리

```sql
SELECT 이름
FROM 회원
WHERE 나이 > (
    SELECT AVG(나이)
    FROM 회원
);
```

-  WHERE 절 서브쿼리가 가장 일반적

---

## 7.2 인라인 뷰 (Inline View)

- FROM 절에서 사용하는 서브쿼리

```sql
SELECT *
FROM (
    SELECT 이름, 나이
    FROM 회원
) AS temp;
```

---

### 특징

- 하나의 테이블처럼 사용
- 반드시 별칭(Alias) 필요

-  MySQL은 FROM 서브쿼리 별칭 필수

---

## 7.3 스칼라 서브쿼리 (Scalar Subquery)

- SELECT 절에서 사용하는 서브쿼리

```sql
SELECT 
    이름,
    (
        SELECT 부서명
        FROM 부서
        WHERE 부서.id = 직원.부서_id
    ) AS 부서명
FROM 직원;
```

---

### 특징

- 반드시 하나의 값만 반환
- 결과 없으면 NULL 가능

- ❗ 여러 행 반환 시 오류 발생

-  스칼라 서브쿼리는 단일 값 반환 필수

---

# 8. 서브쿼리 사용 조건

## 가능 조건

- SELECT 문 사용
- 괄호 `()` 내부 작성

---

## 주의사항

- 서브쿼리 끝에 세미콜론(`;`) 사용 X

```sql
WHERE id = (
    SELECT id
    FROM 회원
)
```

- [확실] 서브쿼리 내부 세미콜론 사용 안함

---

## ORDER BY 제한

- 일반적으로 ORDER BY 단독 사용 불가

```sql
SELECT *
FROM (
    SELECT *
    FROM 회원
    ORDER BY 이름
) AS temp;
```

- LIMIT과 함께 사용하는 경우 허용 가능

- ❗ 기존 설명 보완  
- ORDER BY를 "절대 사용 불가" ❌  
- 특정 상황에서 가능 ✅

-  DBMS별 제한 존재

---

# 9. 서브쿼리 사용 가능한 위치

| 위치 | 사용 가능 여부 |
|------|----------------|
| SELECT | 가능 |
| FROM | 가능 |
| WHERE | 가능 |
| HAVING | 가능 |
| ORDER BY | 가능 |
| INSERT | 가능 |
| UPDATE | 가능 |
| DELETE | 가능 |

---

## INSERT 예시

```sql
INSERT INTO 회원복사
SELECT *
FROM 회원;
```

---

## UPDATE 예시

```sql
UPDATE 회원
SET 등급 = (
    SELECT 등급
    FROM 등급표
    WHERE 조건
);
```

-  서브쿼리는 다양한 SQL 문에서 활용 가능

---

# 10. JOIN vs 서브쿼리

| 구분 | JOIN | 서브쿼리 |
|------|------|----------|
| 특징 | 테이블 연결 | 쿼리 내부 쿼리 |
| 장점 | 성능 유리 가능 | 가독성 좋음 |
| 단점 | 복잡 가능 | 느릴 수 있음 |

-  대용량에서는 JOIN이 더 유리한 경우 많음
---

# 11. 핵심 정리

- 서브쿼리 = SQL 내부 SELECT
- WHERE / FROM / SELECT 절 사용 가능
- 인라인 뷰는 FROM 절 서브쿼리
- 스칼라 서브쿼리는 단일 값 반환

-  서브쿼리는 SQL 핵심 문법 중 하나

---

# 참고

-  SQL 표준에서 서브쿼리 지원
