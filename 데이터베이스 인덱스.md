# 인덱스 (INDEX)

---

# 1. 인덱스란?

- 인덱스(Index)는 데이터 조회 속도를 향상시키기 위한 자료구조이다.

- 추가적인 저장 공간을 사용하여 검색 성능을 높인다.

-  인덱스는 검색 성능 향상을 위한 DB 핵심 기능
-  대부분 B-Tree 기반 구조 사용

---

## 책의 색인(Index) 예시

- 도서관에서 책을 찾을 때
    - 책 전체를 하나씩 찾는 방법 → Full Scan
    - 책 뒤의 색인을 사용하는 방법 → Index Search

- 인덱스는 데이터 위치를 빠르게 찾도록 도와준다.

-  인덱스는 탐색 범위를 줄이는 역할 수행


---
# 2. 인덱스의 문제점

- 인덱스를 무조건 많이 생성한다고 성능이 좋아지는 것은 아니다.

- ❗ 기존 설명 보완  → 인덱스도 관리 비용 존재

---

## 문제점

- 저장 공간 추가 사용
- INSERT/UPDATE/DELETE 성능 저하
- 잘못된 인덱스는 오히려 느려질 수 있음

-  인덱스는 쓰기 작업 시 추가 작업 발생

---

# 3. 인덱스 장단점

---

## 장점

- SELECT 조회 속도 향상 -> 빠른 탐색이 가능하다.
- WHERE 검색 속도 개선
  - ORDER BY / GROUP BY 성능 향상 가능 -> 특정 컬럼의 데이터를 복사하여 정렬한 뒤, 별도의 저장 공간에 보관

-  인덱스는 조회 성능 최적화 핵심 요소

---

## 단점

- 추가 저장 공간 필요
- 쓰기 성능 저하
- 인덱스 생성 시간 필요
- 데이터 변경 시 성능 저하 가능

```text
INSERT → 인덱스 추가 작업 발생
UPDATE → 인덱스 재정렬 가능
DELETE → 인덱스 제거 작업 발생
```

-  인덱스는 조회와 쓰기 성능 Trade-Off 관계

---

# 4. 인덱스 종류 (MySQL 기준)

## 4.1 클러스터형 인덱스 (Clustered Index)

- 실제 데이터가 인덱스 기준으로 정렬 저장

- MySQL InnoDB에서는 PRIMARY KEY가 클러스터형 인덱스 역할 수행

---

### 특징

- 데이터 자체가 정렬됨
- PK 기준 저장
- 테이블당 1개만 존재 가능

-  InnoDB는 PK 기반 클러스터형 인덱스 사용

---

### 인덱스 동작 방식
해당 테이블을 생성시 생성하고 싶은 인덱스 컬럼을 지정한다.

>> 특정 컬럼에 대한 인덱스를 생성하면 해당 컬럼의 데이터을 정렬하여 별도의 메모리 공간에 데이털의 물리적 주소와 함께 저장된다.

### 예시

```sql
CREATE TABLE 회원 (
    id INT PRIMARY KEY,
    이름 VARCHAR(30)
);
```

- id 기준으로 데이터 저장 구조 결정

인덱스 생성도면 해당 컬렘에 대한 where 조건문을 사용하여 쿼리를 날릴 때 옵티마이저에 <br>
의해 판단되어 생성된 인덱스를 탈 수 있게 되고, 인덱스에 저장되어 있는 데이터의 물리적 주소로 가서
데이터를 가져오는 방식으로 동작하여 검색 속도의 향상을 가져올 수 있다.

- 옵티마이저: SQL을 빠르고 효율적으로 수행할 최적의 처리 경로를 생성해주는 DBMS 내부의 핵심 엔진

---

## 4.2 보조 인덱스 (Secondary Index)

- 실제 데이터와 별도로 인덱스 구조 생성

- 데이터를 직접 저장하지 않고 위치 정보 참조

---

### 특징

- 여러 개 생성 가능
- 클러스터형 인덱스를 참조

- ❗ 기존 오류 수정  
  → "정렬되지 않는다" ❌  
  → "별도의 인덱스 구조로 정렬 관리" ✅

-  보조 인덱스도 내부적으로 정렬 구조 사용

---

# 5. 자동 생성되는 인덱스

## PRIMARY KEY

```sql
PRIMARY KEY(id)
```

- 자동으로 인덱스 생성

---

## UNIQUE

```sql
UNIQUE(email)
```

- UNIQUE 제약조건도 인덱스 생성

-  PK/UNIQUE는 자동 인덱스 생성

---

# 6. 인덱스 생성

## 기본 문법

```sql
CREATE INDEX 인덱스명
ON 테이블명(컬럼명);
```

---

## 예시

```sql
CREATE INDEX idx_user_name
ON users(name);
```

-  컬럼 기준 인덱스 생성 가능

---

# 7. 인덱스 삭제

```sql
DROP INDEX 인덱스명 ON 테이블명;
```

-  사용하지 않는 인덱스 제거 가능

---

# 8. 인덱스 사용 시 주의점

## 인덱스가 잘 사용되는 경우

- WHERE 검색
- JOIN
- ORDER BY
- GROUP BY

---

## 인덱스가 잘 사용되지 않는 경우

```sql
WHERE 함수(컬럼)
```

```sql
WHERE LIKE '%검색어'
```

- 앞에 `%` 사용 시 인덱스 사용 어려움

-  함수 사용 시 인덱스 무효화 가능
-  Leading Wildcard는 인덱스 효율 감소

---

# 9. Full Table Scan

- 인덱스를 사용하지 않고 전체 테이블 탐색

```text
테이블 처음 → 끝까지 전부 조회
```

- 데이터가 적으면 오히려 더 빠를 수 있음

-  작은 데이터셋에서는 Full Scan이 유리 가능

---

# 10. 실행계획 확인 (EXPLAIN)

```sql
EXPLAIN
SELECT *
FROM users
WHERE name = 'kim';
```

- 인덱스 사용 여부 확인 가능
-  EXPLAIN은 SQL 실행계획 분석 도구

---

# 11. 핵심 정리

| 종류 | 특징 |
|------|------|
| 클러스터형 인덱스 | 실제 데이터 정렬 |
| 보조 인덱스 | 별도 인덱스 구조 |
| 장점 | 조회 성능 향상 |
| 단점 | 쓰기 성능 감소 |

-  인덱스는 조회 최적화 핵심 기술


---

# 참고

-  MySQL InnoDB는 B-Tree 인덱스 기반
****